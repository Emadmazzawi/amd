
# AMD ROCm PyTorch should be installed separately via the official index
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/rocm6.0

# Core dependencies from AMD PyTorch Fine-tuning Playbook
transformers==4.57.1
safetensors==0.6.2
accelerate>=0.28.0
peft>=0.10.0
trl>=0.8.0
bitsandbytes>=0.43.0
"fsspec[http]>=2023.1.0,<=2025.9.0"

# Dataset and Serving
datasets>=2.18.0
vllm>=0.4.1
streamlit>=1.32.0
